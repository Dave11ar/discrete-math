#include<bits/stdc++.h>

using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  cout.tie(0);

  int n, k;
  cin >> n >> k;
  int dp[100][100];

  n *= 2;
  dp[0][0] = 1;
  int d = 0;

  for (int i = 1; i < n; i++) {
    if (d == 0 || ) {



    }




  }

  return 0;
}
